# Used by `npm run build` (Vite) for the published GitHub Pages site.
# After the email backend is deployed, paste its public URL here (no trailing
# slash), e.g. https://tender-report-email.onrender.com
# Then run `npm run deploy` to publish the frontend that points at it.
VITE_API_BASE=
